# Data-Structure

# Array

1. two very imp concepts
   subarray -- solved using 3 loops --
   subsequence -- solved using recursion -- by taking and not taking logic

# Stack

1. you push the element and pop the element order is reversed.
2. generally can be used to optimize O(N^2) solution , where j dependent on i.
3. LIFO - last in first out.
4. 4 main operation -- push(), pop(), peek(), isEmpty().

   note -- for poping ele, you can add extra conditions also

   while(!stack.isEmpty()){
   stack.pop();
   }

5. Most of the stack problem can be solved using these concepts :
   you can add extra conditions also

   while(!stack.isEmpty()){
   //logic
   }

   if(!stack.isEmpty()){
   //do this
   }else{
   do this
   //stack empty
   }

6. Imp questions --next Greater , next Samller, valid paranthesis, infix to postfix , postfix to infix
